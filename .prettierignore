# Build output & caches
node_modules/
dist/
.astro/
.wrangler/
.netlify/
.vercel/
output/

# Generated artifacts (owned by their generators — never hand-format)
public/citations.json
src/data/citations.ts
package-lock.json

# Markdown corpus + manifests are managed by markdownlint, not prettier
docs/
*.md

# CI / infra config kept hand-managed
.github/

# Harness / tooling state + managed config — not ours to reformat
.claude/
.serena/
.conductor/
.codex/
.specstory/
.gemini/
seed.yaml

# Legacy Cloudflare Pages Functions (superseded by the Astro worker bundle)
functions/

# Cross-client IP isolation (local-only)
.private/

# prettier-plugin-astro@0.14 mishandles these two files (valid Astro, build
# fine): HydrationNode has HTML comments inside `{cond && (<>…</>)}` it can't
# parse; aesthetics formats non-idempotently (--write output fails --check).
# Exempt until the astro plugin handles them; revisit on plugin upgrade.
src/components/HydrationNode.astro
src/pages/aesthetics.astro
